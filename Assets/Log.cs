using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//2017.2.15 之前的工程被unity的示例工程安装时覆盖掉了，今天总算恢复的差不多了，但是player的行走如果一直摁着鼠标右键会有卡顿。
//2017.2.16 今天把血条照着网上的教程用slider做出来了，也查了一下基于层的物体碰撞，需要改动物理引擎里的内容。然后是关于碰撞的接口一共有6个，明天改。

public class Log : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
